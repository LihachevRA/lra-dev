extends ../layout.pug

block title
  title Страница не найдена

block content
  main(class="text-gray-900 antialiased mt-auto")
    div(class="relative overflow-hidden pt-24 pb-12 md:py-0")
      div(class="relative z-10 flex flex-col items-center justify-center px-4 pt-8 md:pt-0")
        div(class="text-center mb-8 md:mb-12")
          div(class="space-y-3 md:space-y-4 mb-6 md:mb-8")
            h1(class="text-4xl md:text-7xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-600 via-purple-600 to-teal-600") Oops!
            h2(class="text-2xl md:text-4xl font-bold text-gray-900") Страница не найдена